{{!< default}}

<div class="content-area">
    <main class="site-main">
        {{#tag}}
            <section class="tag">
                <!-- {{#if feature_image}}
                    <img class="tag-image" srcset="{{> srcset}}" src="{{img_url feature_image size="xs"}}" alt="{{name}}">
                {{/if}} -->
                <div class="container medium">
                    <h1 class="tag-name">{{name}}</h1>
                    {{#if description}}
                        <div class="tag-description">{{description}}</div>
                    {{/if}}
                    <!-- {{#get "tags" limit="all" filter="id:-{{id}}+visibility:public" as |tags|}}
                        {{#if tags}}
                            <div class="tag-list">
                                <span class="tag-list-label">More:</span>
                                {{#foreach tags}}
                                    <a class="tag-list-item" href="{{url}}">{{name}}</a>
                                {{/foreach}}
                            </div>
                        {{/if}}
                    {{/get}} -->
                </div>
            </section>
        {{/tag}}
        <div class="post-feed container small">
            {{#foreach posts visibility="all"}}
                {{> "loop"}}
            {{/foreach}}
        </div>
        {{pagination}}
    </main>
</div>
