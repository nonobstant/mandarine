<nav class="pagination" role="navigation">
    {{#if prev}}
        <a class="newer-posts button-text button-pagination" href="{{page_url prev}}">BACK</a>
    {{/if}}
    <!-- <span class="page-number">Page {{page}} of {{pages}}</span> -->
    <span class="page-number"> </span>
    {{#if next}}
        <a class="older-posts button-text button-pagination" href="{{page_url next}}">NEXT</a>
    {{/if}}
</nav>
